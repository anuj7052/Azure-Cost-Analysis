# Held back deliberately. 0.141 ships a Starlette whose route objects no
# longer expose a writable `.path`, and core/versioning.py builds the /api/v1
# mirror by copying routes and rewriting that attribute. Under 0.141 the
# mirror is never built: every /api/v1 route disappears, taking its auth
# dependencies with it, and tests/test_team.py catches it. Moving up is a
# rewrite of _versioned(), not a version bump.
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-dotenv==1.0.1
pydantic==2.13.5
pydantic-settings==2.15.0
httpx==0.28.1
msal==1.38.0
PyJWT[crypto]==2.13.0
cryptography==50.0.1
aiosqlite==0.22.1
# Held at 2.x: pandas 3.0 changes copy-on-write and string dtype defaults,
# which the CSV and BOQ parsers read into directly.
pandas==2.2.3
python-multipart==0.0.12
aiohttp==3.14.3
python-dateutil==2.9.0
openpyxl==3.1.5
pdfplumber==0.11.4
openai==3.8.0
reportlab>=4.0

# Production WSGI/ASGI server for App Service. One worker on purpose: the
# SQLite database and the module-level caches are per-process, so a second
# worker would mean two divergent caches and two writers on one file.
gunicorn==23.0.0
asyncpg==0.31.0
