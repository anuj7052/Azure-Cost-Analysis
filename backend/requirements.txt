fastapi==0.115.0
uvicorn[standard]==0.30.6
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.5.2
httpx==0.27.2
msal==1.31.0
PyJWT[crypto]==2.9.0
cryptography==43.0.1
aiosqlite==0.20.0
pandas==2.2.3
python-multipart==0.0.12
aiohttp==3.10.10
python-dateutil==2.9.0
openpyxl==3.1.5
pdfplumber==0.11.4
openai==3.6.0
reportlab>=4.0

# Production WSGI/ASGI server for App Service. One worker on purpose: the
# SQLite database and the module-level caches are per-process, so a second
# worker would mean two divergent caches and two writers on one file.
gunicorn==23.0.0
asyncpg==0.30.0
